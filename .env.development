# Unified dev (`npm run dev`) serves UI + API on PORT (default 8787). Leave unset so the
# client uses same-origin `/api/chat`.
# To call the API host directly from the browser (separate origin), set e.g.:
# VITE_CHAT_API_URL=http://localhost:8787
