# Optional: full origin of the chat API (no trailing slash; do not append /api — paths like /api/chat are added automatically).
# For `npm run dev`, the repo includes `.env.development` with a direct URL to the
# local API (so Network shows port 8787). Override here or in `.env.local` if needed.
# VITE_CHAT_API_URL=http://localhost:8787
